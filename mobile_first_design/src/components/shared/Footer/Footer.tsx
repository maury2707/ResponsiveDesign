import styles from './Footer.module.sass';

export const Footer = () => {
    return(
        <footer className={styles.footer}>
            información legal general, fecha de creación
        </footer>
    )
}